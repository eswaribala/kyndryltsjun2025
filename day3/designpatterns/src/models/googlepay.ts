export class GooglePay {
  
  public  processPaymentUsingGP(amount: number, currency: string): any {
    // Simulate a payment processing with Google Pay
    
        console.log(`Payment of ${amount} ${currency} processed successfully with Google Pay.`);
  }
}